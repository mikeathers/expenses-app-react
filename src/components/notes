

AddExpenseForm

    ExpenseForm
      set state from expenseData passed up from child, use state data to add expense

        TravelExpenseForm
          set state from expenseData passed up from child, pass that expenseData up to parent as prop

            OdometerForm
              Set state for form values, onChange event on each textbox pass the new state up to parent as prop

            GoogleMapsForm
              Set state for form values, onChange event on each textbox pass the new state up to parent as prop
